describe('Router', function () {
     it("provides the 'router' object", function () {
       // Expect exists and is an object.
       expect(router).to.be.an("object");

});
     });

     describe('Menu', function () {
     it("provides the 'menu' object", function () {
       // Expect exists and is an object.
       expect(menu).to.be.an("object");

});
});

     describe('Payment', function () {
     it("provides the 'payment' object", function () {
       // Expect exists and is an object.
       expect(payment).to.be.an("object");

});
});

     describe('Cart', function () {
     it("provides the 'cart' object", function () {
       // Expect exists and is an object.
       expect(cart).to.be.an("object");

});
});

 describe('Order', function () {
     it("provides the 'order' object", function () {
       // Expect exists and is an object.
       expect(order).to.be.an("object");

});
});

 describe('Profile', function () {
     it("provides the 'profile' object", function () {
       // Expect exists and is an object.
       expect(profile).to.be.an("object");

});
});

describe('Registration', function () {
     it("provides the 'registration' object", function () {
       // Expect exists and is an object.
       expect(registration).to.be.an("object");

});
});






// describe("Test timing", function () {
//      it("should be a fast test", function (done) {
//        expect("hi").to.equal("hi");
// done(); });
//      it("should be a medium test", function (done) {
//  setTimeout(function () {
//       expect("hi").to.equal("hi");
//       done();
// }, 40); });
//   it("should be a slow test", function (done) {
//     setTimeout(function () {
//       expect("hi").to.equal("hi");
//       done();
//     }, 100);
// });
//   it("should be a timeout failure", function (done) {
//     setTimeout(function () {
//       expect("hi").to.equal("hi");
//       done();
//     }, 2001);
// });

// });
